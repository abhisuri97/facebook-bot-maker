// Generated by CoffeeScript 1.9.1
(function() {
  var URLHash, crypto, extend,
    bind = function(fn, me){ return function(){ return fn.apply(me, arguments); }; };

  extend = require('deep-extend');

  crypto = require('crypto');

  URLHash = (function() {
    function URLHash(options) {
      this.hash = bind(this.hash, this);
      this.options = extend({}, options, {
        key: 'abcdeg',
        algorithm: 'sha1',
        encoding: 'hex'
      });
    }

    URLHash.prototype.hash = function(url) {
      var hash, hashProvider, options;
      options = this.options;
      hashProvider = crypto.createHmac(options.algorithm, options.key);
      hashProvider.setEncoding(options.encoding);
      hashProvider.write(url);
      hashProvider.end();
      hash = hashProvider.read();
      return hash;
    };

    return URLHash;

  })();

  module.exports = URLHash;

}).call(this);

//# sourceMappingURL=index.js.map
